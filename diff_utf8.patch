diff --git a/backend/agent/executor.py b/backend/agent/executor.py
index 14aab51..83dcbaf 100644
--- a/backend/agent/executor.py
+++ b/backend/agent/executor.py
@@ -1757,7 +1757,7 @@ def run_agent(
                 "decision": "Direct Dynamic Greeting"
             })
 
-        token_usage = sink.totals()
+        token_usage = sink.totals(config=config)
         calls_log = sink.get_calls_log()
         execution_trace = list(_trace_sink)
         total_turn_sec = round(time.time() - turn_t0, 2)
@@ -1817,7 +1817,7 @@ def run_agent(
             "search_shortcircuit": False,
         })
 
-    token_usage = sink.totals()
+    token_usage = sink.totals(config=config)
     calls_log = sink.get_calls_log()
     execution_trace = list(_trace_sink)
     total_turn_sec = round(time.time() - turn_t0, 2)
diff --git a/backend/core/models.py b/backend/core/models.py
index a6217ad..2f193b6 100644
--- a/backend/core/models.py
+++ b/backend/core/models.py
@@ -166,6 +166,16 @@ class JinaEmbeddingsAPIClient:
             response = _post_with_retry(self.url, headers=headers, json_data=data, timeout=30)
             res_json = response.json()
 
+            if "usage" in res_json:
+                try:
+                    from backend.core.usage import record
+                    usage = res_json["usage"]
+                    prompt_tokens = usage.get("prompt_tokens", 0)
+                    if prompt_tokens > 0:
+                        record("embedding", input_tokens=prompt_tokens, output_tokens=0, model=self.model_name, provider="jina")
+                except Exception:
+                    pass
+
             # Ensure correct ordering based on response indices
             sorted_data = sorted(res_json["data"], key=lambda x: x["index"])
             all_embeddings.extend([item["embedding"] for item in sorted_data])
@@ -223,6 +233,16 @@ class OpenAIEmbeddingsAPIClient:
             response = _post_with_retry(self.url, headers=headers, json_data=data, timeout=30)
             res_json = response.json()
 
+            if "usage" in res_json:
+                try:
+                    from backend.core.usage import record
+                    usage = res_json["usage"]
+                    prompt_tokens = usage.get("prompt_tokens", 0)
+                    if prompt_tokens > 0:
+                        record("embedding", input_tokens=prompt_tokens, output_tokens=0, model=self.model_name, provider="openai")
+                except Exception:
+                    pass
+
             # Ensure correct ordering based on response indices
             sorted_data = sorted(res_json["data"], key=lambda x: x["index"])
             all_embeddings.extend([item["embedding"] for item in sorted_data])
diff --git a/backend/core/usage.py b/backend/core/usage.py
index f626228..4648a24 100644
--- a/backend/core/usage.py
+++ b/backend/core/usage.py
@@ -44,6 +44,7 @@ class _Sink:
     def __init__(self) -> None:
         self._lock = threading.Lock()
         self.by_kind: dict[str, dict] = {}
+        self.by_model: dict[str, dict] = {}
         self._context_tokens = 0  # largest single call's input_tokens this run
         self._calls_log: list[dict] = []  # raw prompt/response audit trail, this run
 
@@ -65,6 +66,13 @@ class _Sink:
             ctx = int(context_tokens if context_tokens is not None else it)
             if ctx > self._context_tokens:
                 self._context_tokens = ctx
+                
+            if model:
+                m = self.by_model.setdefault(model, {
+                    "input_tokens": 0, "output_tokens": 0
+                })
+                m["input_tokens"] += it
+                m["output_tokens"] += ot
 
             # Audit log: only recorded when the caller passes prompt/raw_response ΓÇö
             # optional, since not every record() call site has been wired up to
@@ -80,12 +88,41 @@ class _Sink:
         with self._lock:
             return list(self._calls_log)
 
-    def totals(self) -> dict:
+    def totals(self, config=None) -> dict:
         with self._lock:
             ti = sum(v["input_tokens"] for v in self.by_kind.values())
             to = sum(v["output_tokens"] for v in self.by_kind.values())
             tr = sum(v["reasoning_tokens"] for v in self.by_kind.values())
             calls = sum(v["calls"] for v in self.by_kind.values())
+            
+            total_cost = 0.0
+            model_costs = {}
+            cfg_dict = config.raw if hasattr(config, "raw") else config
+            
+            # Populate model_costs with tokens first (so they appear even if cost is 0)
+            for model, usage in self.by_model.items():
+                model_costs[model] = {
+                    "input_tokens": usage["input_tokens"],
+                    "output_tokens": usage["output_tokens"],
+                    "input_cost": 0.0,
+                    "output_cost": 0.0,
+                    "total_cost": 0.0,
+                }
+            
+            if isinstance(cfg_dict, dict) and "models_cost" in cfg_dict:
+                costs = cfg_dict["models_cost"]
+                for model, usage in self.by_model.items():
+                    if model in costs:
+                        c = costs[model]
+                        it_cost = (usage["input_tokens"] / 1_000_000.0) * c.get("input", 0.0)
+                        ot_cost = (usage["output_tokens"] / 1_000_000.0) * c.get("output", 0.0)
+                        cost = it_cost + ot_cost
+                        total_cost += cost
+                        
+                        model_costs[model]["input_cost"] = it_cost
+                        model_costs[model]["output_cost"] = ot_cost
+                        model_costs[model]["total_cost"] = cost
+
             return {
                 "calls": calls,
                 "input_tokens": ti,
@@ -93,7 +130,9 @@ class _Sink:
                 "reasoning_tokens": tr,
                 "total_tokens": ti + to,
                 "context_tokens": self._context_tokens,
+                "total_cost_usd": total_cost,
                 "by_kind": {k: dict(v) for k, v in self.by_kind.items()},
+                "by_model": model_costs,
             }
 
 
diff --git a/backend/pipeline/graph.py b/backend/pipeline/graph.py
index 6a8b656..63e55f3 100644
--- a/backend/pipeline/graph.py
+++ b/backend/pipeline/graph.py
@@ -281,7 +281,7 @@ def run_pipeline(tools, state: PipelineState, config, on_step=None) -> PipelineS
         with usage.using_sink() as sink:
             final = graph.invoke(state)
         if isinstance(final, dict):
-            final["token_usage"] = sink.totals()
+            final["token_usage"] = sink.totals(config=cfg)
             final["_calls_log"] = sink.get_calls_log()
         return final
 
@@ -300,7 +300,7 @@ def run_pipeline(tools, state: PipelineState, config, on_step=None) -> PipelineS
                     pass
                 seen += 1
     if isinstance(final, dict):
-        final["token_usage"] = sink.totals()
+        final["token_usage"] = sink.totals(config=cfg)
         final["_calls_log"] = sink.get_calls_log()
     return final
 
diff --git a/config/global.yaml b/config/global.yaml
index a315ba4..42d2023 100644
--- a/config/global.yaml
+++ b/config/global.yaml
@@ -1,180 +1,129 @@
-# Global pipeline configuration.
-# Override per-deployment by copying this file and passing the new path to load_config().
-# Secrets (API keys, DB passwords) go in .env ΓÇö never in this file.
-# Database connection strings use ${VAR} substitution resolved from the environment.
-
-# ΓöÇΓöÇ LLM ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# provider: groq | google | ollama | openai
-# `openai` + base_url reaches ANY OpenAI-compatible API (NVIDIA NIM, OpenRouter,
-# Together, z.ai, DeepSeek, vLLM, ΓÇª). api_key is usually a ${ENV_VAR}; blank => SDK's
-# default env.
-# Recipes:
-#   OpenAI (active): provider: openai  model: gpt-4o-mini  base_url: https://api.openai.com/v1  api_key: ${OPENAI_API_KEY}
-#   z.ai GLM:        provider: openai  model: glm-4.7-flash
-#                    base_url: https://api.z.ai/api/paas/v4/  api_key: ${ZAI_API_KEY}
-#   Groq:            provider: groq    model: llama-3.1-8b-instant   api_key: ${GROQ_API_KEY}
-#   Gemini (AI Studio): provider: google  model: gemini-2.5-flash   api_key: ${GOOGLE_API_KEY}
-#   DeepSeek:        provider: openai  model: deepseek-chat
-#                    base_url: https://api.deepseek.com/v1           api_key: ${DEEPSEEK_API_KEY}
-#   NVIDIA NIM:      provider: openai  model: meta/llama-3.1-8b-instruct
-#                    base_url: https://integrate.api.nvidia.com/v1   api_key: ${NVIDIA_API_KEY}
-#   OpenRouter:      provider: openai  model: <org/model>
-#                    base_url: https://openrouter.ai/api/v1          api_key: ${OPENROUTER_API_KEY}
-#   Local vLLM:      provider: openai  model: <served>  base_url: http://localhost:8001/v1
 llm:
   provider: openai
   model: gpt-4o-mini
   answer_model: gpt-4o-mini
   base_url: https://api.openai.com/v1
   api_key: ${OPENAI_API_KEY}
-
-# ΓöÇΓöÇ Per-step model overrides (MODULAR) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Any step below may pin its OWN model (and optionally provider/api_key/base_url/
-# temperature); anything unset inherits the global llm block above. Cost/quality knob:
-# run bulk enrichment on a cheap model, answering on a stronger one, etc. Resolution is
-# always: step.model -> (answer_model for plan/answer) -> global llm.model.
-#   categorization.model     categorize/classify document_type + industry
-#   enrichment.model         per-chunk summary + keywords (bulk ΓÇö biggest cost lever)
-#   query.planner.model      query rewriting/decomposition
-#   query.answerer.model     final grounded answer
-# Handled in code by get_llm_for(config, section) ΓÇö see backend/core/llm_client.py.
-
-# ΓöÇΓöÇ Categorization ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Used by CategorizeTool.
-# config["categorization"]["industry_keywords"] ΓÇö keyword lists per industry
-# config["categorization"]["confidence_thresholds"] ΓÇö when to flag low confidence
 categorization:
   confidence_thresholds:
     categorization_low_confidence: 0.5
-
   industry_keywords:
     automotive:
-      - toyota
-      - ford
-      - bmw
-      - vehicle
-      - engine
-      - torque
-      - transmission
-      - chassis
-      - automotive
+    - toyota
+    - ford
+    - bmw
+    - vehicle
+    - engine
+    - torque
+    - transmission
+    - chassis
+    - automotive
     electronics:
-      - circuit
-      - pcb
-      - schematic
-      - voltage
-      - resistor
-      - capacitor
-      - signal
-      - semiconductor
+    - circuit
+    - pcb
+    - schematic
+    - voltage
+    - resistor
+    - capacitor
+    - signal
+    - semiconductor
     manufacturing:
-      - assembly
-      - drawing
-      - tolerance
-      - weld
-      - machining
-      - fixture
-      - jig
-      - bom
-      - part number
+    - assembly
+    - drawing
+    - tolerance
+    - weld
+    - machining
+    - fixture
+    - jig
+    - bom
+    - part number
     finance:
-      - balance sheet
-      - revenue
-      - profit
-      - ledger
-      - audit
-      - fiscal
-      - equity
-      - financial statement
+    - balance sheet
+    - revenue
+    - profit
+    - ledger
+    - audit
+    - fiscal
+    - equity
+    - financial statement
     legal:
-      - contract
-      - agreement
-      - clause
-      - liability
-      - indemnity
-      - arbitration
-      - jurisdiction
+    - contract
+    - agreement
+    - clause
+    - liability
+    - indemnity
+    - arbitration
+    - jurisdiction
     healthcare:
-      - patient
-      - diagnosis
-      - clinical
-      - pharma
-      - dosage
-      - trial
-      - medical
-      - drug
-      - therapy
+    - patient
+    - diagnosis
+    - clinical
+    - pharma
+    - dosage
+    - trial
+    - medical
+    - drug
+    - therapy
     retail_ecommerce:
-      - order id
-      - add to cart
-      - delivery address
-      - shipped
-      - return policy
-      - sku
-      - cart total
-      - mrp
+    - order id
+    - add to cart
+    - delivery address
+    - shipped
+    - return policy
+    - sku
+    - cart total
+    - mrp
     travel_transport:
-      - pnr
-      - boarding point
-      - seat number
-      - departure time
-      - ticket number
-      - passenger name
-      - bus operator
-      - coach number
+    - pnr
+    - boarding point
+    - seat number
+    - departure time
+    - ticket number
+    - passenger name
+    - bus operator
+    - coach number
     entertainment_media:
-      - showtime
-      - screen number
-      - movie ticket
-      - booking id
-      - seat no
-      - show timing
-      - multiplex
-      - cinema hall
+    - showtime
+    - screen number
+    - movie ticket
+    - booking id
+    - seat no
+    - show timing
+    - multiplex
+    - cinema hall
     general: []
-
-# ΓöÇΓöÇ Vision ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# provider: google (AI Studio) | ollama (self-hosted) | openai (any OpenAI-compatible
-# multimodal endpoint via base_url ΓÇö GPT-4o-vision, NVIDIA NIM VLMs, OpenRouter).
 vision:
   provider: openai
   model: meta/llama-3.2-11b-vision-instruct
   base_url: https://integrate.api.nvidia.com/v1
   api_key: ${NVIDIA_API_KEY}
   enabled: true
-  dpi: 150                  # render resolution for PDF image regions
-  min_image_px: 150         # images smaller than this (each side) are skipped
+  dpi: 150
+  min_image_px: 150
   timeout_s: 120
-  max_retries: 5            # retry/backoff is the safety net for transient 429/5xx
-  max_concurrency: 2        # concurrency limit for vision processing
-  min_interval_s: 2         # pacing between consecutive calls to prevent rate-limit hits
-  # Focus instructions, selected by route first, then industry, then "default"
-  # (see backend/vision/prompts.py:build_vision_prompt). Add a customer's industry
-  # key here to tune captions without code changes.
+  max_retries: 5
+  max_concurrency: 2
+  min_interval_s: 2
   prompt:
-    default: >
-      Describe what this image shows in 2-3 sentences. Be specific about the type of
-      visual (diagram, chart, photo, schematic) and capture component labels, part
-      numbers, dimensions, and any visible text VERBATIM (do not paraphrase numbers).
-    automotive: >
-      You are analyzing a page from a vehicle/equipment service manual. Capture
-      exploded views and assembly diagrams, part names and part numbers, callout
-      numbers, fasteners, torque/clearance specs, and any labels, warnings, or
-      measurements. Be precise ΓÇö this is used for parts/maintenance search.
-    cad_route: >
-      Describe all visible circuit components, reference designators (R1, C4),
-      net names (e.g. 5V_RAIL, GND), connector types, pin counts, and any
-      visible dimensions or tolerances. Be precise ΓÇö this is used for search.
+    default: 'Describe what this image shows in 2-3 sentences. Be specific about the
+      type of visual (diagram, chart, photo, schematic) and capture component labels,
+      part numbers, dimensions, and any visible text VERBATIM (do not paraphrase numbers).
 
-# ΓöÇΓöÇ Vision-OCR (page transcription for COMPLEX pages) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Routed VLM transcription: when native text is garbled (broken fonts) or a page has
-# tables/figures, render the page and let a document VLM transcribe it (clean text +
-# correct markdown tables) ΓÇö what pymupdf/OCR get wrong. Runs ONLY on flagged pages
-# (mode: complex), so it's cheap. provider: openai covers any OpenAI-compatible VLM
-# API (below: NVIDIA Build API for testing). Set mode: off to disable, always to force.
+      '
+    automotive: 'You are analyzing a page from a vehicle/equipment service manual.
+      Capture exploded views and assembly diagrams, part names and part numbers, callout
+      numbers, fasteners, torque/clearance specs, and any labels, warnings, or measurements.
+      Be precise ΓÇö this is used for parts/maintenance search.
+
+      '
+    cad_route: 'Describe all visible circuit components, reference designators (R1,
+      C4), net names (e.g. 5V_RAIL, GND), connector types, pin counts, and any visible
+      dimensions or tolerances. Be precise ΓÇö this is used for search.
+
+      '
 vision_ocr:
   enabled: true
-  mode: always              # off (native/Paddle only) | complex (hybrid: cheap + VLM on hard pages) | always (VLM-ONLY)
+  mode: always
   provider: openai
   model: meta/llama-3.2-11b-vision-instruct
   base_url: https://integrate.api.nvidia.com/v1
@@ -183,43 +132,28 @@ vision_ocr:
   min_interval_s: 2
   max_retries: 3
   timeout_s: 120
-
-# ΓöÇΓöÇ OCR (scanned pages) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Engine picked per deployment (surya|paddle ΓÇö see extraction/scanned_pdf/).
-# engine: surya | paddle
-#   surya  ΓÇö torch + llama.cpp; ONE pass gives text + layout, 90+ languages, best
-#            accuracy on complex layouts. Needs llama-server binary. Best on GPU.
-#   paddle ΓÇö PaddleOCR text + DocLayout-YOLO/contour regions. CNN recognition,
-#            reliable and fast on CPU. DEFAULT for CPU/dev.
 ocr:
-  engine: paddle            # CPU default.
-  layout_engine: auto       # auto | surya | contour
-  surya_timeout_s: 90       # Surya bounds per page before falling back to PaddleOCR
-  subprocess_timeout_s: 1800 # Overall OCR process timeout
-
-# ΓöÇΓöÇ Extraction platform (Docling) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Docling (IBM, MIT) is the maintained document-AI engine: layout model + reading
-# order, TableFormer for real table structure, OCR for scanned pages, figure
-# localization. ONE tool (docling_pdf) covers digital/scanned/mixed. It runs on CPU
-# (dev) and auto-accelerates on a CUDA GPU ΓÇö same code, just set device.
-# Also supports dedicated remote extraction microservice via mode: server.
+  engine: paddle
+  layout_engine: auto
+  surya_timeout_s: 90
+  subprocess_timeout_s: 1800
 extraction:
-  deskew: true                # auto-rotate sideways scans via Tesseract OSD
-  stitch_tables: true         # merge tables that continue across page breaks
-  stitch_arbitration: true    # ask LLM to arbitrate ambiguous table continuations
+  deskew: true
+  stitch_tables: true
+  stitch_arbitration: true
   docling:
-    do_ocr: false             # Docling internal OCR (false = use VLM rescue / Paddle)
-    do_table_structure: true  # TableFormer: recover rows/cols (vs flat text)
-    images_scale: 2.0         # internal render scale for layout/figures
-    min_picture_pts: 24       # skip regions smaller than this on a side
+    do_ocr: false
+    do_table_structure: true
+    images_scale: 2.0
+    min_picture_pts: 24
     min_picture_area_frac: 0.004
-    figure_union: true        # union Docling + DocLayout-YOLO figures
-    page_rescue: true         # per-page VLM rescue for garbled/scanned pages
-    max_vlm_pages: 60         # budget cap for VLM page transcriptions per doc
-    device: auto              # auto | cpu | cuda | mps
+    figure_union: true
+    page_rescue: true
+    max_vlm_pages: 60
+    device: auto
     num_threads: 4
-    mode: local               # local (in-process) | server (remote Docling microservice)
-    server_url: ${DOCLING_SERVER_URL} # remote Docling server endpoint (e.g. http://100.49.250.232:8083)
+    mode: local
+    server_url: http://100.49.250.232:8083
     server_key: ${DOCLING_API_KEY}
   by_kind:
     digital:
@@ -230,53 +164,42 @@ extraction:
       table_source: auto
     default:
       table_source: auto
-  # Large-format / diagram pages (CAD, circuit schematics, oversized engineering sheets).
-  # Tiles high-DPI renders to preserve legible component designators.
   large_format:
     enabled: true
     dpi: 200
     vlm_max_px: 2600
     overlap: 0.12
-    merge: llm                # llm = dedup-merge tiles via LLM; concat = join
+    merge: llm
     max_tiles: 24
   cad:
-    max_pages: 0              # cap VLM pages per CAD/circuit doc (0 = unlimited)
+    max_pages: 0
     vision:
       provider: google
       model: gemini-3.5-flash
       api_key: ${GOOGLE_API_KEY}
-      min_interval_s: 4.0      # Pacing to stay within AI Studio free-tier RPM limits
+      min_interval_s: 4.0
       max_retries: 3
       timeout_s: 150
       dpi: 200
-
-# ΓöÇΓöÇ Embeddings & Reranker ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Provider can be "openai", "local" (SentenceTransformers), or "jina" (cloud API).
-# Dense dimension, models, and rerankers swap automatically via Settings UI.
 embeddings:
-  dense_provider: openai                # openai | local | jina
-  dense_model: text-embedding-3-small   # OpenAI text-embedding-3-small, BAAI/bge-m3, or jina-embeddings-v3
+  dense_provider: openai
+  dense_model: text-embedding-3-small
   dense_api_key: ${OPENAI_API_KEY}
-  dense_dim: 1024                       # Matryoshka output dimension for text-embedding-3-small
-  dense_document_prefix: ''             # prefix required for some models (e.g. nomic)
+  dense_dim: 1024
+  dense_document_prefix: ''
   dense_query_prefix: ''
   dense_batch_size: 16
-  sparse_model: Qdrant/bm25             # keyword matching for hybrid search
-  reranker_provider: jina               # jina | local
-  reranker_model: jina-reranker-v2-base-multilingual # jina-reranker-v2-base-multilingual or BAAI/bge-reranker-v2-m3
+  sparse_model: Qdrant/bm25
+  reranker_provider: jina
+  reranker_model: jina-reranker-v2-base-multilingual
   reranker_api_key: ${JINA_API_KEY}
-  chunking_model: minishlab/potion-base-32M # SemanticChunker split bounds model
-
-# ΓöÇΓöÇ Chunking ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+  chunking_model: minishlab/potion-base-32M
 chunking:
-  strategy: semantic        # semantic | recursive
-  size: 400                 # tokens per chunk
-  overlap: 50               # token overlap
-  section_aware: true       # break chunks at section headings & preserve document hierarchy
-  split_large_tables: true  # split tables larger than size by row group, repeating headers
-
-# ΓöÇΓöÇ Enrichment ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# enrich_chunks stamps tags + summary + semantic search terms on each chunk.
+  strategy: semantic
+  size: 400
+  overlap: 50
+  section_aware: true
+  split_large_tables: true
 enrichment:
   summarize: true
   keyword_count: 6
@@ -286,82 +209,75 @@ enrichment:
   model: gpt-4o-mini
   base_url: https://api.openai.com/v1
   api_key: ${OPENAI_API_KEY}
-  prompt: >
-    You label document chunks for a technical search index. For each chunk produce a one-sentence
-    summary stating what it is about and what a reader would learn (in the document's own terms), plus
-    6-10 SPECIFIC search terms taken from the chunk ΓÇö part numbers, model names, component/procedure names,
-    specs and units, domain terms. Never use stopwords, generic words (information, section, manual),
+  prompt: 'You label document chunks for a technical search index. For each chunk
+    produce a one-sentence summary stating what it is about and what a reader would
+    learn (in the document''s own terms), plus 6-10 SPECIFIC search terms taken from
+    the chunk ΓÇö part numbers, model names, component/procedure names, specs and units,
+    domain terms. Never use stopwords, generic words (information, section, manual),
     or terms not present in the chunk.
 
-# ΓöÇΓöÇ Query Pipeline ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    '
 query:
-  small_corpus_tokens: 100000   # below this threshold, adaptive_router skips retrieval
-  max_context_tokens: 0         # answer_tool context budget (0 = dynamic)
+  small_corpus_tokens: 100000
+  max_context_tokens: 0
   steps:
-    - query_planner
-    - retrieval
-    - answerer
+  - query_planner
+  - retrieval
+  - answerer
   planner:
-    max_sub_questions: 4        # QueryPlannerTool: cap on decomposed sub-questions
+    max_sub_questions: 4
   answerer: null
   hyde:
-    enabled: false              # generate hypothetical answer before encoding
+    enabled: false
   retrieval:
-    method: hybrid_rerank       # naive | hybrid | hybrid_rerank | hyde | enriched
-    top_n: 20                   # fused chunks returned by plain hybrid
-    candidate_k: 80             # wide candidate pool for reranker scoring
-    rerank_top_k: 5             # chunks kept after reranking
-    dense_weight: 0.6           # RRF weight for dense vector leg
-    sparse_weight: 0.4          # RRF weight for sparse/BM25 leg
-    fallback_enabled: true      # expand to full page when top score is low
-    fallback_threshold: -1.0    # bge-reranker logit below this triggers page expansion
-    fallback_top_k: 2           # number of low-score chunks to expand into full pages
+    method: hybrid_rerank
+    top_n: 20
+    candidate_k: 80
+    rerank_top_k: 5
+    dense_weight: 0.6
+    sparse_weight: 0.4
+    fallback_enabled: true
+    fallback_threshold: -1.0
+    fallback_top_k: 2
   agent:
-    max_iterations: 8           # maximum tool-calling reasoning turns
-    max_history_messages: 20    # sliding window of conversation messages
+    max_iterations: 8
+    max_history_messages: 20
     provider: openai
     model: gpt-4o-mini
     base_url: https://api.openai.com/v1
     api_key: ${OPENAI_API_KEY}
     extra_body: {}
     write_tools:
-      - ingest_document         # human approval required before running write tools
-
-# ΓöÇΓöÇ Document Taxonomy ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    - ingest_document
 document_types:
-  - invoice
-  - purchase_order
-  - financial_statement
-  - report
-  - research_paper
-  - manual
-  - contract
-  - policy
-  - datasheet
-  - presentation
-  - spreadsheet
-  - cad_drawing
-  - circuit_diagram
-  - schematic
-  - image
-  - unknown
-
+- invoice
+- purchase_order
+- financial_statement
+- report
+- research_paper
+- manual
+- contract
+- policy
+- datasheet
+- presentation
+- spreadsheet
+- cad_drawing
+- circuit_diagram
+- schematic
+- image
+- unknown
 industries:
-  - automotive
-  - electronics
-  - manufacturing
-  - finance
-  - legal
-  - healthcare
-  - retail_ecommerce
-  - travel_transport
-  - entertainment_media
-  - general
-
+- automotive
+- electronics
+- manufacturing
+- finance
+- legal
+- healthcare
+- retail_ecommerce
+- travel_transport
+- entertainment_media
+- general
 default_industry: general
-
-# ΓöÇΓöÇ Routing ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Maps document_type ΓåÆ route name. The route selects which pipeline steps run.
 type_to_route:
   cad_drawing: cad_route
   circuit_diagram: circuit_route
@@ -379,106 +295,94 @@ type_to_route:
   spreadsheet: text_default
   image: image_route
   unknown: text_default
-
 extractors:
   excel: excel_extraction
   ppt: ppt_extraction
   docx: word_extraction
   image: image_extraction
-
 pdf_extractors:
-  digital: docling_pdf
+  digital: pymupdf_pdf
   scanned: docling_pdf
   mixed: docling_pdf
-
 route_extractors:
   cad_route: cad_extract
   circuit_route: cad_extract
-
 routes:
   text_default:
     steps:
-      - categorize
-      - extract
-      - chunk
-      - enrich_chunks
-      - embed
-      - index
+    - categorize
+    - extract
+    - chunk
+    - enrich_chunks
+    - embed
+    - index
   diagram_heavy:
     steps:
-      - categorize
-      - extract
-      - vision_enrichment
-      - chunk
-      - enrich_chunks
-      - embed
-      - index
+    - categorize
+    - extract
+    - vision_enrichment
+    - chunk
+    - enrich_chunks
+    - embed
+    - index
   cad_route:
     steps:
-      - categorize
-      - extract
-      - cad_chunk_llm
-      - embed
-      - index
+    - categorize
+    - extract
+    - cad_chunk_llm
+    - embed
+    - index
   circuit_route:
     steps:
-      - categorize
-      - extract
-      - cad_chunk_llm
-      - embed
-      - index
+    - categorize
+    - extract
+    - cad_chunk_llm
+    - embed
+    - index
   image_route:
     steps:
-      - categorize
-      - extract
-      - vision_enrichment
-      - chunk
-      - enrich_chunks
-      - embed
-      - index
-
-# ΓöÇΓöÇ Ingestion Profile ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-ingestion:
-  steps:
     - categorize
     - extract
     - vision_enrichment
     - chunk
-    - cad_chunk_llm
     - enrich_chunks
     - embed
     - index
+ingestion:
+  steps:
+  - categorize
+  - extract
+  - vision_enrichment
+  - chunk
+  - cad_chunk_llm
+  - enrich_chunks
+  - embed
+  - index
   route_gates:
     vision_enrichment:
-      - text_default
-      - diagram_heavy
-      - image_route
+    - text_default
+    - diagram_heavy
+    - image_route
     chunk:
-      - text_default
-      - diagram_heavy
-      - image_route
+    - text_default
+    - diagram_heavy
+    - image_route
     enrich_chunks:
-      - text_default
-      - diagram_heavy
-      - image_route
+    - text_default
+    - diagram_heavy
+    - image_route
     cad_chunk_llm:
-      - cad_route
-      - circuit_route
-
-# ΓöÇΓöÇ Database & Vector Storage ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    - cad_route
+    - circuit_route
 database:
   postgres_url: ${POSTGRES_URL}
   qdrant_url: ${QDRANT_URL}
   qdrant_api_key: ${QDRANT_API_KEY}
   qdrant_collection: chunks
-
-# ΓöÇΓöÇ Directory Auto-Ingestion Watcher ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
 auto_ingestion:
   enabled: true
   watch_dir: auto_ingest
   poll_interval: 10
-
-# ΓöÇΓöÇ Guardrails (v5 Configuration) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
 guardrails:
   enabled: true
   version: 1.0.0
@@ -551,14 +455,33 @@ guardrails:
     interval_seconds: 60
   cost:
     enabled: true
-    llm_input_cost_per_1m: 0.075
-    llm_output_cost_per_1m: 0.3
-    embed_cost_per_1m: 0.0
-
-# ΓöÇΓöÇ Cloud Object Storage (Supabase) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-# Used for storing original document uploads and converted assets.
+models_cost:
+  gpt-4o-mini:
+    input: 0.15
+    output: 0.6
+  gpt-4o:
+    input: 2.5
+    output: 10.0
+  llama-3.1-8b-instruct:
+    input: 0.0
+    output: 0.0
+  meta/llama-3.1-8b-instruct:
+    input: 0.0
+    output: 0.0
+  gemini-2.5-flash:
+    input: 0.3
+    output: 2.5
+  meta/llama-3.2-11b-vision-instruct:
+    input: 0.0
+    output: 0.0
+  text-embedding-3-small:
+    input: 0.02
+    output: 0.0
+  jina-embeddings-v3:
+    input: 0.0
+    output: 0.0
 storage:
   provider: supabase
   supabase_url: ${SUPABASE_URL}
   supabase_key: ${SUPABASE_SERVICE_KEY}
-  supabase_bucket: documents
\ No newline at end of file
+  supabase_bucket: documents
diff --git a/frontend/src/components/ChatPage.jsx b/frontend/src/components/ChatPage.jsx
index 155846e..8ab81e5 100644
--- a/frontend/src/components/ChatPage.jsx
+++ b/frontend/src/components/ChatPage.jsx
@@ -206,6 +206,8 @@ const Tooltip = ({ children, content }) => {
   );
 };
 
+const USD_TO_INR = 83.50;
+
 const ChatPage = () => {
   const [searchParams] = useSearchParams();
   const [sidebarOpen, setSidebarOpen] = useState(false);
@@ -214,6 +216,7 @@ const ChatPage = () => {
   const [sessions, setSessions] = useState([]);
   const [sessionId, setSessionId] = useState(newSessionId);
   const [messages, setMessages] = useState([]);
+  const [currency, setCurrency] = useState('USD');
   const [input, setInput] = useState('');
   const [loadingSessions, setLoadingSessions] = useState({});
   const loading = !!loadingSessions[sessionId];
@@ -1492,7 +1495,9 @@ const ChatPage = () => {
                 onViewPages={(pages) => setPageViewer({ pages, activeIdx: 0 })}
                 onIngestApprove={handleIngestApprove}
                 onIngestCancel={handleIngestCancel}
-                allFiles={allFiles} />
+                allFiles={allFiles}
+                currency={currency}
+                setCurrency={setCurrency} />
             ))}
 
             {messages.length > 0 && messages[messages.length - 1].role === 'user' && (
@@ -2396,7 +2401,7 @@ const CustomCodeBlock = ({ language, value }) => {
   );
 };
 
-const MessageRow = ({ msg, onApprove, onDecline, onClarify, loading, onViewPages, onIngestApprove, onIngestCancel, allFiles }) => {
+const MessageRow = ({ msg, onApprove, onDecline, onClarify, loading, onViewPages, onIngestApprove, onIngestCancel, allFiles, currency, setCurrency }) => {
   const isUser = msg.role === 'user';
   const sources = !isUser ? parseSources(msg.toolCalls, allFiles) : [];
   const rawListed = !isUser ? parseListedDocuments(msg.toolCalls) : {
@@ -2779,6 +2784,16 @@ const MessageRow = ({ msg, onApprove, onDecline, onClarify, loading, onViewPages
                       <CircleStackIcon className="h-3.5 w-3.5" />
                       Tokens: {msg.tokenUsage.total_tokens}
                     </span>
+                    {msg.tokenUsage.total_cost_usd !== undefined && (
+                      <button
+                        onClick={() => setCurrency(currency === 'USD' ? 'INR' : 'USD')}
+                        className="btn-primary-pill !py-0.5 !px-2 !text-[10px] ml-auto font-bold inline-flex items-center gap-1"
+                      >
+                        {currency === 'USD'
+                          ? `Cost: $${msg.tokenUsage.total_cost_usd.toFixed(4)}`
+                          : `Cost: Γé╣${(msg.tokenUsage.total_cost_usd * USD_TO_INR).toFixed(2)}`}
+                      </button>
+                    )}
                     <span>Input: {msg.tokenUsage.input_tokens}</span>
                     <span>Output: {msg.tokenUsage.output_tokens}</span>
                     {msg.tokenUsage.reasoning_tokens > 0 && (
diff --git a/frontend/src/components/IngestionPage.jsx b/frontend/src/components/IngestionPage.jsx
index fa6aa2c..eec3109 100644
--- a/frontend/src/components/IngestionPage.jsx
+++ b/frontend/src/components/IngestionPage.jsx
@@ -14,6 +14,7 @@ import {
   Cog6ToothIcon,
   ChevronDownIcon,
   ClockIcon,
+  CurrencyDollarIcon,
 } from '@heroicons/react/24/outline';
 
 // The real ingestion steps (match tool `name`s in the metrics the API returns).
@@ -69,11 +70,14 @@ const fmtLlmCallsSub = (tu) => {
   return `${total} LLM calls`;
 };
 
+const USD_TO_INR = 83.50;
+
 const IngestionPage = () => {
   const navigate = useNavigate();
   const [files, setFiles] = useState([]);
-  const [uploading, setUploading] = useState(false);
   const [loading, setLoading] = useState(true);
+  const [uploading, setUploading] = useState(false);
+  const [currency, setCurrency] = useState('USD');
   const [error, setError] = useState(null);
   const [serverConnected, setServerConnected] = useState(false);
   const [uploadProgress, setUploadProgress] = useState({});
@@ -613,15 +617,30 @@ const IngestionPage = () => {
             <h2 className="text-2xl font-bold text-[#4a154b] display-title">
               Processed Document Library {files.length > 0 && `(${files.length})`}
             </h2>
-            {files.length > 0 && (
-              <button
-                onClick={loadFiles}
-                className="btn-secondary-pill flex items-center gap-2"
-              >
-                <ArrowPathIcon className="h-4 w-4" />
-                Refresh List
-              </button>
-            )}
+            {files.length > 0 && (() => {
+              const totalCostUSD = files.reduce((sum, f) => sum + (f.token_usage?.total_cost_usd || 0), 0);
+              return (
+                <div className="flex items-center gap-3">
+                  <button
+                    onClick={() => setCurrency(currency === 'USD' ? 'INR' : 'USD')}
+                    className="btn-primary-pill flex items-center gap-2 font-bold"
+                  >
+                    <CurrencyDollarIcon className="h-4 w-4" />
+                    {currency === 'USD' 
+                      ? `Total: $${totalCostUSD.toFixed(4)}` 
+                      : `Total: Γé╣${(totalCostUSD * USD_TO_INR).toFixed(2)}`
+                    }
+                  </button>
+                  <button
+                    onClick={loadFiles}
+                    className="btn-secondary-pill flex items-center gap-2"
+                  >
+                    <ArrowPathIcon className="h-4 w-4" />
+                    Refresh List
+                  </button>
+                </div>
+              );
+            })()}
           </div>
 
           {loading ? (
@@ -670,19 +689,30 @@ const IngestionPage = () => {
                       </div>
                       <div className="flex-1 min-w-0">
                         <div className="flex items-center gap-3 flex-wrap">
-                          <h3 className="font-bold text-[#4a154b] truncate text-base md:text-lg display-title">{file.filename}</h3>
-                          <span className="text-[10px] font-extrabold text-[#696969] px-2.5 py-0.5 rounded-full bg-[#f4ede4] uppercase tracking-wider">{file.file_type || 'Document'}</span>
+                          <h3 className="font-semibold text-[#4a154b] truncate text-sm max-w-[200px] md:max-w-[400px]">{file.filename}</h3>
+                          <span className="text-[9px] font-extrabold text-[#696969] px-2 py-0.5 rounded-full bg-[#f4ede4] uppercase tracking-wider">{file.file_type || 'Document'}</span>
                           
                           {/* Total Time Badge */}
                           {(() => {
                             const totalMs = (file.metrics || []).reduce((sum, m) => sum + (m.ms || 0), 0);
                             return totalMs > 0 ? (
-                              <span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-[#f9f0ff] border border-[#e6e6e6] text-xs font-bold text-[#4a154b]">
-                                <ClockIcon className="h-3.5 w-3.5 text-[#4a154b]" />
-                                Total Time: {fmtDuration(totalMs)}
+                              <span className="inline-flex items-center gap-1 px-2.5 py-0.5 rounded-full bg-[#f9f0ff] border border-[#e6e6e6] text-[10px] font-bold text-[#4a154b]">
+                                <ClockIcon className="h-3 w-3 text-[#4a154b]" />
+                                {fmtDuration(totalMs)}
                               </span>
                             ) : null;
                           })()}
+
+                          {/* Total Cost Badge */}
+                          {file.token_usage?.total_cost_usd !== undefined && (
+                            <span className="inline-flex items-center gap-1 px-2.5 py-0.5 rounded-full bg-[#f9f0ff] border border-[#e6e6e6] text-[10px] font-bold text-[#4a154b]">
+                              <CurrencyDollarIcon className="h-3 w-3 text-[#4a154b]" />
+                              {currency === 'USD' 
+                                ? `$${file.token_usage.total_cost_usd.toFixed(4)}`
+                                : `Γé╣${(file.token_usage.total_cost_usd * USD_TO_INR).toFixed(2)}`
+                              }
+                            </span>
+                          )}
                         </div>
                       </div>
                     </div>
@@ -751,6 +781,32 @@ const IngestionPage = () => {
                           </div>
                         )}
 
+                        {/* Row 1.5: Costs */}
+                        {file.token_usage?.by_model && Object.keys(file.token_usage.by_model).length > 0 && (
+                          <div className="flex flex-wrap gap-2.5 border-t border-[#e6e6e6] pt-3">
+                            <StatBlock 
+                              label="Total Cost" 
+                              value={currency === 'USD' 
+                                ? `$${(tu.total_cost_usd || 0).toFixed(4)}`
+                                : `Γé╣${((tu.total_cost_usd || 0) * USD_TO_INR).toFixed(2)}`} 
+                            />
+                            {Object.entries(file.token_usage.by_model)
+                              .filter(([model]) => !model.toLowerCase().includes('embedding'))
+                              .map(([model, metrics]) => (
+                                <StatBlock 
+                                  key={model} 
+                                  label={model.split('/').pop()} 
+                                  value={currency === 'USD'
+                                    ? `$${(metrics.total_cost || 0).toFixed(4)}`
+                                    : `Γé╣${((metrics.total_cost || 0) * USD_TO_INR).toFixed(2)}`} 
+                                  sub={currency === 'USD'
+                                    ? `in: $${(metrics.input_cost || 0).toFixed(4)} ┬╖ out: $${(metrics.output_cost || 0).toFixed(4)}`
+                                    : `in: Γé╣${((metrics.input_cost || 0) * USD_TO_INR).toFixed(4)} ┬╖ out: Γé╣${((metrics.output_cost || 0) * USD_TO_INR).toFixed(4)}`} 
+                                />
+                            ))}
+                          </div>
+                        )}
+
                         {/* Row 2: Step Timings */}
                         {(totalMs > 0 || (file.metrics && file.metrics.length > 0)) && (
                           <div className="flex flex-wrap gap-2.5 border-t border-[#e6e6e6] pt-3">
