# STUB (owner: Karthii). Bring up the local data plane with one command.
# Fill in the images/ports for the vector DB, relational DB, and object store
# once those are chosen. Keep all of this self-hosted.
services: {}
#  vector-db:        { image: "<chosen-vector-db>",  ports: ["6333:6333"] }
#  relational-db:    { image: "postgres:16",         ports: ["5432:5432"], environment: {} }
#  object-store:     { image: "<s3-compatible>",     ports: ["9000:9000"] }
